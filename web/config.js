// API base URL — the live Railway API (T24). API base is config, not hardcoded
// in app.js; swap this one line to re-point the UI. For local dev, run the API
// on :8000 and set this back to "http://localhost:8000".
window.API_BASE = "https://doc-extractor-production-e024.up.railway.app";
